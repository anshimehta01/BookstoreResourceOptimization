/* import { createMuiTheme } from '@material-ui/core/styles';

export const myTheme = createMuiTheme({

    palette: {

        primary: {
            main:'#624af9'
        },
        romance: {
            main: '#CD1E28'
        }
    }
}) */
/* 
    import { createMuiTheme } from '@material-ui/core/styles';

export const myTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#624af9', // violet-blue
    },
    secondary: {
      main: '#CD1E28', // red (used in buttons, highlights)
    },
    background: {
      default: '#f9f9f9',
    },
  },
  typography: {
    fontFamily: 'Poppins, Roboto, sans-serif',
    h2: {
      fontWeight: 600,
    },
    h3: {
      fontWeight: 500,
    },
  },
}); */
/* 
import { createMuiTheme } from '@material-ui/core/styles';

export const myTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#5D4037', // Sepia brown
    },
    secondary: {
      main: '#8D6E63', // Faded burgundy
    },
    background: {
      default: '#FBE9E7', // Antique paper
    },
  },
  typography: {
    fontFamily: 'Georgia, Poppins, serif',
    h2: {
      fontWeight: 600,
    },
    h3: {
      fontWeight: 500,
    },
    body1: {
      fontSize: '1rem',
    },
  },
}); */

import { createMuiTheme } from '@material-ui/core/styles';

export const myTheme = createMuiTheme({

    palette: {

        primary: {
            main:'#5D4037',
        },
        romance: {
            main: '#8D6E63',
        }
    }

})
